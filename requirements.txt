# OrcaMail server dependencies
# web3 / eth-account / requests / websocket-client / cryptography are used by
# the /api/aivm proxy only.  Core OrcaMail routes use Python stdlib only.
web3
eth-account
requests
websocket-client
cryptography
